{
  "name": "@mnemo/github-action",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action for adding to and searching a Mnemo workspace.",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --license licenses.txt",
    "lint": "eslint src --ext ts",
    "test": "vitest run",
    "type-check": "tsc --noEmit"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/getmnemo/getmnemo-github-action.git"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@mnemo/memory": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
